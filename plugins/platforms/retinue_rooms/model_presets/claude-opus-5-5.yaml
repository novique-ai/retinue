# Cloud: Anthropic Claude Opus 5.5 via workspace API key or OAuth.
# Versioned like the other Opus preset so the sidebar switcher, which shows
# the preset name alone, still says which Opus a retainer is on.
model:
  default: claude-opus-5-5
  provider: anthropic
