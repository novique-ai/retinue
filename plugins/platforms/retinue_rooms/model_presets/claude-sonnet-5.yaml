# Cloud: Anthropic Claude Sonnet 5 via workspace API key or OAuth.
# Versioned like the grok and Opus presets so the sidebar switcher, which shows
# the preset name alone, still says which Sonnet a retainer is on.
model:
  default: claude-sonnet-5
  provider: anthropic
